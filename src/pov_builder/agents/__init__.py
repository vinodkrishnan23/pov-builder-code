"""Real per-agent logic lands here, one phase at a time, replacing the
corresponding placeholder in `pov_builder.graph.nodes`. Empty in Phase 0 —
new.md: "Do not implement the actual agent logic" this phase.
"""
