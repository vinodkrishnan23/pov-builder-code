"""The graph package: `state.py` (POVState), `routing.py` (conditional
edges), `builder.py` (`build_graph()`). See `nodes.py` for the Phase 0
placeholder node implementations — real per-agent logic replaces these one
phase at a time (new.md's own recommended build order)."""
